#!/usr/bin/env bash

if [ -n "${PINENTRY_RBW_FIELD-}" ]; then
    rbw unlock || exit 1
    password=$(rbw get "${PINENTRY_RBW_FIELD}")
    echo "OK Pleased to meet you"
    echo "D $password"
    echo "OK"
else
    pinentry "$@"
fi
