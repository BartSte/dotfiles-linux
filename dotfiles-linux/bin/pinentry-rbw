#!/usr/bin/env bash

set -eou pipefail

if [ -n "${PINENTRY_USE_RBW-}" ]; then
    rbw unlock || exit 1
    echo "OK Pleased to meet you"
    echo "D $(rbw get gpg)"
    echo "OK"
else
    pinentry "$@"
fi
