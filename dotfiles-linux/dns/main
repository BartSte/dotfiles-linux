#!/usr/bin/env bash

set -euo pipefail
this_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$this_dir/helpers"

reset_dns
lg "Configuring DNS using NetworkManager to block adult content"
"$this_dir/using_nm/main"
