#!/usr/bin/env bash

set -eo pipefail

if [ -z "$BWEMAIL" ]; then
    lg "BWEMAIL is not set (skipping bw login)"
    exit 0
fi

# Non-interactive in main; login lives in bitwarden/auth
lg "Bitwarden CLI configured (login via bitwarden/auth)"
