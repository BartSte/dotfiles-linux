#!/usr/bin/env bash
# This script is used to launch WINBROWSER on Windows when linux is running
# on wsl. Otherwise, LINUXBROWSER is launched.
running_wsl && "$WINBROWSER" "$@" &>'/dev/null' || "$LINUXBROWSER" "$@" &>'/dev/null'
