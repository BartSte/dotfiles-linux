#!/bin/bash
# This script is used to launch qutebrowser on Windows when linux is running 
# on wsl. Otherwise, qutebrowser is launched normally.

if running_wsl; then
    /mnt/c/Program\ Files/qutebrowser/qutebrowser.exe $(wslpath -w "$@") &
else
    qutebrowser "$@" &
fi

